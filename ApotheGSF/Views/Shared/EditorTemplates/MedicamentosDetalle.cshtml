@model ApotheGSF.Clases.MedicamentosDetalle

@for (int j = 0; j < Model.CajasId.Count; j++)
{
    @Html.HiddenFor(model => Model.CajasId[j])
}

<tr>
    <td>
        <input type="hidden" asp-for="NombreMedicamento" />
        @Model.NombreMedicamento
    </td>
    <td>
        <input type="hidden" asp-for="TipoCantidad" />
        @if (Model.TipoCantidad == 1)
        {
            @:Cajas
        }
        else
        {
            @:Unidades
        }
    </td>
    <td>
        <input type="hidden" asp-for="Cantidad" />
        @Model.Cantidad
    </td>
    <td>
        <input type="hidden" asp-for="Precio" />
        @Model.Precio
    </td>
    <td>
        <input type="hidden" asp-for="Total" />
        @Model.Total
    </td>
    <td>
        @*<input class="btn btn-danger" type="button" id="RemoverMedicamento" value="Remover" />*@
        <i class="fas fa-trash-alt" type="button" style="color:red;" id="RemocerMedicamento"></i>
    </td>
</tr>